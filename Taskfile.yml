version:
  '3'

tasks:
  build:
    desc: "Build the project"
    cmds:
      - go build -o bbcli
    deps:
      - generate

  generate:
    desc: "Generate code"
    cmds:
      - go generate ./...
    sources:
      - app/bbclient/bbclient*
      - app/bbclient/config.yaml
    generates:
      - app/bbclient/*.gen.go